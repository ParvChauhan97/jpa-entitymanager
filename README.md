# JPA EntityManager CRUD Demo

This project demonstrates how to perform CRUD (Create, Read, Update, Delete) operations using JPA EntityManager in a Spring Boot application.

Developed a Spring Boot backend application implementing CRUD operations using JPA EntityManager and Hibernate. The project demonstrates entity lifecycle management, persistence context handling, transaction management, and MySQL database integration while following RESTful API principles.
